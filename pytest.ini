[pytest]
addopts = --strict-markers
markers =
    video_ffmpeg: invokes local FFmpeg or ffprobe
    video_slow: runs a longer render or performance acceptance test
    video_external: explicitly invoked external-service acceptance test; excluded from normal local and CI runs
