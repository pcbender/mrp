---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import SEOHead from "../components/SEOHead.astro";

interface Props {
  title: string;
  description: string;
  paneled?: boolean;
  ogType?: string;
  ogImage?: string;
}

const { title, description, paneled = false, ogType, ogImage } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <SEOHead title={title} description={description} ogType={ogType} ogImage={ogImage} />
  </head>
  <body>
    <Header />
    <main>
      {paneled ? <div class="main-panel"><slot /></div> : <slot />}
    </main>
    <Footer />
  </body>
</html>
